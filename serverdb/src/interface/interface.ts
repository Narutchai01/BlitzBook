export interface IConfig {
    port : number;
    RDS_HOSTNAME : string;
    RDS_USERNAME : string;
    RDS_PASSWORD : string;
    RDS_DB_NAME : string;
    RDS_PORT : number;
    supabaseURL: string;
    supabaseKey: string;
    origin: string;
}